コンテナの利用
----------------------------

バックグラウンドでコンテナを起動する

`docker run -tid --name mycentos centos bash`{{execute}}

起動したコンテナの確認

`docker ps`{{execute}}

コンテナに接続する

`docker attach mycentos`{{execute}}


